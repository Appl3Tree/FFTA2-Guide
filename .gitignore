node_modules
dist
.DS_Store
audit/data-audit.json

# Keep temporary research captures outside version control.
audit/research/

# Local environment files. Keep Firebase project config and any future secrets
# out of git; commit .env.example instead.
.env
.env.local
.env.*.local

# Firebase local/debug output.
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
